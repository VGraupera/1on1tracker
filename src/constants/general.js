/**
 * @description Sort by name constant
 * @const {string}
 * @default
 */
export const SORT_BY_NAME = 'name';

/**
 * @description Sort by team constant
 * @const {string}
 * @default
 */
export const SORT_BY_TEAM_NAME = 'teamName';

/**
 * @description use instead empty team name
 * @const {string}
 * @default
 */
export const SORT_WITHOUT_TEAM_NAME = 'SORT_WITHOUT_TEAM_NAME';

/**
 * @description prefix for archived actions
 * @const {string}
 * @default
 */
export const ARCHIVED_ACTION_PREFIX = 'ARCHIVED_';

/**
 * @description archived prefix for firebase paths
 * @const {string}
 * @default
 */
export const ARCHIVED_PATH_PREFIX = 'archived/';
